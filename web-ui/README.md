# Idea Developer Web UI

Multi-Agent 아이디어 개발 시스템의 웹 인터페이스입니다.

## 아키텍처

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Web UI (Next.js)                              │
├────────────────────────┬─────────────────────────────────────────────┤
│   💬 Chat Interface    │   📊 Workflow Visualization (React Flow)   │
└────────────────────────┴─────────────────────────────────────────────┘
                              │ WebSocket
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                     Backend (FastAPI)                                │
│   - WebSocket 실시간 통신                                             │
│   - 에이전트 오케스트레이션                                           │
│   - 상태 관리 및 진행 브로드캐스트                                    │
└──────────────────────────────────────────────────────────────────────┘
```

## 기술 스택

| 레이어 | 기술 |
|--------|------|
| Frontend | Next.js 14, TypeScript, Tailwind CSS |
| 워크플로우 시각화 | React Flow (@xyflow/react) |
| 아이콘 | Lucide React |
| 실시간 통신 | WebSocket (socket.io-client) |
| Backend | FastAPI, Python 3.11+ |
| AI | Anthropic Claude API |

## 설치 및 실행

### 1. Frontend 설치

```bash
cd web-ui/frontend
npm install
npm run dev
```

Frontend가 http://localhost:3000 에서 실행됩니다.

### 2. Backend 설치

```bash
cd web-ui/backend

# 가상환경 생성 (권장)
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 서버 실행
uvicorn app.main:app --reload
```

Backend가 http://localhost:8000 에서 실행됩니다.

### 3. 환경 변수 설정

```bash
# web-ui/backend/.env
ANTHROPIC_API_KEY=your_api_key_here
```

## 프로젝트 구조

```
web-ui/
├── frontend/                   # Next.js 프론트엔드
│   ├── src/
│   │   ├── app/               # Next.js App Router
│   │   ├── components/
│   │   │   ├── chat/          # 채팅 UI 컴포넌트
│   │   │   └── workflow/      # React Flow 시각화
│   │   └── types/             # TypeScript 타입 정의
│   ├── package.json
│   └── tailwind.config.ts
│
└── backend/                    # FastAPI 백엔드
    ├── app/
    │   ├── api/               # REST/WebSocket 엔드포인트
    │   ├── agents/            # 에이전트 오케스트레이터
    │   └── models/            # Pydantic 스키마
    └── requirements.txt
```

## 워크플로우

1. **채팅으로 아이디어 입력**: 좌측 패널에서 아이디어 입력
2. **실시간 진행 시각화**: 우측 패널에서 에이전트 상태 확인
3. **단계별 업데이트**: WebSocket으로 실시간 상태 수신

### 워크플로우 단계

| Phase | Agents | 실행 방식 |
|-------|--------|----------|
| Research | researcher, competitor, persona | **병렬** |
| Development | expander → critic → refiner → feasibility → validator | 순차 |
| Strategy | monetization → pitch | 순차 |

## API 엔드포인트

### WebSocket

- `ws://localhost:8000/ws` - 실시간 워크플로우 통신

### REST

- `GET /` - API 상태 확인
- `GET /health` - 헬스 체크
- `POST /api/develop` - 아이디어 개발 요청 (비스트리밍)

## 개발

### Frontend 개발

```bash
cd web-ui/frontend
npm run dev      # 개발 서버
npm run build    # 프로덕션 빌드
npm run lint     # 린트 검사
```

### Backend 개발

```bash
cd web-ui/backend
uvicorn app.main:app --reload  # 개발 서버 (자동 리로드)
```

## 라이센스

See [LICENSE](../LICENSE) for details.
